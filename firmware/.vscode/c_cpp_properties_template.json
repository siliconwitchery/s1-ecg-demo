// VS Code Configuration for Intellisense checking.
//
// Find out about what intellisense does here:
// https://code.visualstudio.com/docs/editor/intellisense
//
// Adjust this file for you own paths. "includePath"
// should include the nordic SDK, S1 SDK, your project
// files, and any external libraries you may use. The
// ** includes all files recursivly from that folder.
//
// Be sure to set "compilerPath" to your ARM GCC install
// path.
//
// Add defines for any macros which are specified within
// the makefile or called from the command line after make.
// Intellisense will only include pre-processor directives
// found within .c and .h files, not from makefiles.
//
{
    "configurations": [{
        "name": "ARM",
        "includePath": [
            "${workspaceFolder}/**",
            "${env:HOME}/nRF5SDK/components/**",
            "${env:HOME}/nRF5SDK/external/**",
            "${env:HOME}/nRF5SDK/integration/**",
            "${env:HOME}/nRF5SDK/modules/**"
        ],
        "defines": [
            "\"__STATIC_INLINE=static inline\""
        ],
        "compilerPath": "/Applications/ARM/bin/arm-none-eabi-gcc",
        "cStandard": "c11",
        "cppStandard": "c++17",
        "intelliSenseMode": "gcc-arm"
    }],
    "version": 4
}